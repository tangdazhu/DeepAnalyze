# 数据分析任务 - Student Loan 数据分析

**你的任务**：对学生贷款数据进行完整的数据分析。数据包含 **CSV/Excel 文件** 和 **SQLite 数据库**。

**🚨 核心规则 - 必须严格遵守 🚨**

1. **第 2-6 轮：必须使用 `pd.read_csv()` 读取 CSV 文件**
   - ❌ **禁止使用 `sqlite3.connect()` 或 `pd.read_sql_query()`**
   - ✅ **必须使用 `pd.read_csv(CSV_PATH)`**
   
2. **第 7 轮：使用 SQLite 数据库进行多表关联**
   - ✅ 此时才允许使用 `sqlite3.connect()`

3. **第 10 轮：输出 `<Answer>`**
   - ❌ **第 10 轮之前禁止输出 `<Answer>`**
   - ✅ **只有第 10 轮才能输出 `<Answer>`**

**如果你在第 10 轮之前输出 `<Answer>`，系统会强制终止任务并报错。**

## ⚡ 强制执行规则

**每轮分析完成后的行为**：
1. 输出 `</Code>` 后**立即停止**,不要输出任何其他内容
2. 等待系统执行代码并返回结果
3. 看到执行结果后,**立即**开始下一轮分析
4. **禁止**在轮次之间输出任何解释、总结或等待指令
5. **禁止**返回空响应,必须立即输出下一轮的 `<Analyze>` 和 `<Code>`

**示例流程**：
```
第2轮: <Analyze>...</Analyze> <Code>...</Code>
系统: <Execute>...</Execute> <File>...</File>
第3轮: <Analyze>...</Analyze> <Code>...</Code>  ← 立即开始,不要等待
系统: <Execute>...</Execute> <File>...</File>
第4轮: <Analyze>...</Analyze> <Code>...</Code>  ← 立即开始,不要等待
...
```

**看到Bootstrap执行结果后,立即开始分析,不要等待用户指令。**

## 数据背景

这是一个关于学生贷款的数据集，包含学生的人口统计信息、教育记录、财务状况和就业状态等信息。

## 数据文件清单

**重要说明**：
- 每个数据集都有 **CSV 和 XLSX 两个版本**，内容相同，可任选其一
- SQLite 数据库包含所有数据，表名与文件名对应（如 `enrolled` 表 = `enrolled.csv`）

**10 个数据集**（每个都有 `.csv` 和 `.xlsx` 两个文件）：
1. `bool` - 布尔标记（仅包含 name 列，3 条记录）
2. `person` - 学生名单（仅包含 name 列，1000 名学生）
3. `disabled` - 残疾学生（name 列）
4. `enlist` - 入伍记录（name, organ 列）
5. `filed_for_bankrupcy` - 破产记录（name 列）
6. `longest_absense_from_school` - 最长缺勤记录（name, month 列）
7. `male` - 男性学生（name 列，存在即为男性）
8. `no_payment_due` - 还款记录（name, bool 列）
9. `unemployed` - 失业学生（name 列）
10. `enrolled` - 入学记录（name, school, month 列，最大文件）

**SQLite 数据库**：
- `student_loan.sqlite` (300KB) - 包含上述 10 个表，表名与文件名一致

**数据关系**：
- 所有表通过 `name` 字段关联（学生姓名）
- `person` 表是主表，包含所有学生
- 其他表是子集，只包含特定条件的学生

**重要**：第一轮执行后，系统会显示所有表的完整字段列表。**必须严格使用第一轮结果中的字段名**，不要根据文件名推测字段。

## 必须遵守的核心流程

### 1. 输出格式（每一轮必须严格遵守）

**你必须立即输出以下格式，不要输出任何其他内容**：

```
<Analyze>
一句话说明本轮分析目标（不超过30字）
</Analyze>

<Code>
完整的 Python 代码
</Code>
```

**关键规则**：
- ✅ 第一轮 bootstrap 后，**立即输出 `<Analyze>` 和 `<Code>`**
- ✅ `<Analyze>` 只写分析目标，不解释错误、不讨论方法
- ✅ 输出 `</Code>` 后**立即停止**，不输出任何内容
- ❌ **禁止输出任何解释性文字、问候语、询问语句**
- ❌ **禁止说"已成功获取"、"请提出需求"等话**
- ❌ 禁止在 `<Analyze>` 中解释上一轮错误
- ❌ 禁止在 `</Code>` 后继续输出
- ❌ 禁止输出 `<Execute>` 或 `<File>` 标签

### 2. 第一轮：系统自动完成

**重要**：第一轮由系统自动执行 bootstrap，已在 `<Execute>` 块中显示：
1. **CSV 数据文件的绝对路径**（用于第 2-6 轮分析）
2. **SQLite 数据库表结构**（用于第 7 轮多表关联分析）

**你的任务**：仔细阅读第一轮的 `<Execute>` 输出，记住：
- **CSV 文件绝对路径**（如 `enrolled.csv`, `enlist.csv` 等）
- **SQLite 数据库绝对路径**（`DB_PATH`，用于第 7 轮）
- **所有表名和字段名**（用于第 7 轮的 SQL 查询）
- 禁止使用不存在的表名或字段名

**第一轮后，立即开始第二轮分析，不要等待用户指令，不要询问用户需求**。

### 3. 分析流程（必须按顺序完成）

- **第 2 轮**：分析 enrolled.csv 文件 📄
- **第 3 轮**：分析 no_payment_due.csv 文件 📄
- **第 4 轮**：分析 longest_absense_from_school.csv 文件 📄
- **第 5 轮**：分析 enlist.csv 文件 📄
- **第 6 轮**：分析 disabled.csv 文件 📄
- **第 7 轮**：使用 SQLite 数据库进行多表关联分析 📊
- **第 8 轮**：生成 README.md 索引文件（记录所有生成的文件及其说明）
- **第 9 轮**：生成 comprehensive_analysis_report.md（综合数据分析报告）
- **第 10 轮**：输出 `<Answer>`（唯一允许输出 Answer 的轮次）

### 4. 第 2-6 轮：CSV 文件单表分析（必须完成）

**🚨 强制要求 - 必须严格遵守 🚨**：
- 必须完成 **5 轮 CSV 文件分析**
- **每轮只分析一个表,生成指定的文件后立即进入下一轮**
- **禁止对同一个表进行多次分析**
- **禁止跳过任何轮次**
- **使用第 1 轮 Bootstrap 输出的 CSV 文件绝对路径**
- 分析以下 5 个 CSV 文件（**必须按此顺序执行**）：
  - **第 2 轮**：`enrolled.csv`（字段：name, school, month）→ 生成 enrolled_summary.csv + enrolled_school_dist.png
  - **第 3 轮**：`no_payment_due.csv`（字段：name, bool）→ 生成 payment_status_summary.csv + payment_status_dist.png
  - **第 4 轮**：`longest_absense_from_school.csv`（字段：name, month）→ 生成 absense_summary.csv + absense_month_dist.png
  - **第 5 轮**：`enlist.csv`（字段：name, organ）→ 生成 enlist_summary.csv + enlist_organ_dist.png
  - **第 6 轮**：`disabled.csv`（字段：name）→ 生成 disabled_count.csv + disabled_vs_total.png
- 每轮必须生成 **恰好** 1 个 CSV 文件和 1 个 PNG 图表
- 所有文件保存到 `generated/` 目录

**完成标准**：
- ✅ 生成了指定的 CSV 和 PNG 文件
- ✅ 立即进入下一轮,分析下一个表
- ❌ 不要对同一个表进行额外的深入分析
- ❌ 不要尝试分析多个维度或生成额外的文件

**🚨 数据读取方式（第2-6轮）🚨**：
- ✅ **必须使用 `pd.read_csv(CSV_PATH)` 读取CSV文件**
- ❌ **严禁使用 `sqlite3.connect()` 或 `pd.read_sql_query()`**
- ❌ **严禁使用 `DB_PATH` 变量**
- ✅ 第 7 轮才允许使用 SQLite 数据库
- ✅ CSV文件路径格式: `/home/tdz/DeepAnalyze/demo/workspace/session_xxx/文件名.csv`

**示例：第 2 轮分析 enrolled.csv 文件**

```
<Analyze>
分析 enrolled.csv 的学校分布和入学时间
</Analyze>

<Code>
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
from pathlib import Path

# 使用第 1 轮提供的 CSV 文件绝对路径
CSV_PATH = r"/home/tdz/DeepAnalyze/demo/workspace/session_xxx/enrolled.csv"
OUTPUT_DIR = Path("generated")
OUTPUT_DIR.mkdir(parents=True, exist_ok=True)

# 读取 CSV 文件
df = pd.read_csv(CSV_PATH)

# 统计学校分布
school_counts = df['school'].value_counts().reset_index()
school_counts.columns = ['school', 'count']
school_counts.to_csv(OUTPUT_DIR / "enrolled_summary.csv", index=False, encoding='utf-8')

# 可视化学校分布
plt.figure(figsize=(10, 6))
sns.barplot(data=school_counts.head(15), x='count', y='school', palette='viridis')
plt.title("前15所入学学校学生数量分布")
plt.xlabel("学生数量")
plt.ylabel("学校")
plt.tight_layout()
plt.savefig(OUTPUT_DIR / "enrolled_school_dist.png", dpi=120)
plt.close()

print(f"分析完成：enrolled.csv 共 {len(df)} 条记录，{len(school_counts)} 所学校")
</Code>
```

**第 3 轮示例：分析 no_payment_due.csv 文件**

```
<Analyze>
分析 no_payment_due.csv 的还款状态分布
</Analyze>

<Code>
import pandas as pd
import matplotlib.pyplot as plt
from pathlib import Path

# 使用第 1 轮提供的 CSV 文件绝对路径
CSV_PATH = r"/home/tdz/DeepAnalyze/demo/workspace/session_xxx/no_payment_due.csv"
OUTPUT_DIR = Path("generated")
OUTPUT_DIR.mkdir(parents=True, exist_ok=True)

# 读取 CSV 文件
df = pd.read_csv(CSV_PATH)

# 统计还款状态
payment_stats = df['bool'].value_counts().reset_index()
payment_stats.columns = ['status', 'count']
payment_stats['percentage'] = payment_stats['count'] / len(df) * 100
payment_stats.to_csv(OUTPUT_DIR / "payment_status_summary.csv", index=False, encoding='utf-8')

# 可视化
plt.figure(figsize=(6, 4))
plt.pie(payment_stats['count'], labels=payment_stats['status'], autopct='%1.1f%%')
plt.title("学生还款状态分布")
plt.tight_layout()
plt.savefig(OUTPUT_DIR / "payment_status_dist.png", dpi=120)
plt.close()

print(f"分析完成：no_payment_due.csv 共 {len(df)} 条记录")
</Code>
```

**第 4-6 轮**：继续分析其他 CSV 文件，每个文件生成对应的统计结果和可视化图表。

### 5. 第 7 轮：SQLite 多表关联分析

**重要**：第 7 轮开始使用 SQLite 数据库，不再使用 CSV 文件。

**目的**：合并多个表，发现数据之间的关联。

**强制要求**：
- ✅ 使用第 1 轮 Bootstrap 提供的 SQLite 数据库绝对路径
- ✅ 使用 SQL JOIN 语句进行多表关联
- ✅ 严格使用第 1 轮显示的表名和字段名
- ✅ **每张表在 set_index/reindex 之前必须先 `drop_duplicates(subset=['name'])`**，确保唯一键再做 JOIN，避免 pandas duplicate labels 报错
- ✅ **合并结果中必须保留 `name` 列**（不要 `set_index('name')` 后又访问 `name`，若已设为索引请 `reset_index()`）
- ✅ `longest_absense_from_school.month` 需重命名为 `absense_month`（例如 `df.rename(columns={'month': 'absense_month'})`），`no_payment_due.bool` 需映射为 `payment_status`
- ❌ **禁止使用 "Join"、"Merged"、"Result" 等作为表名**
- ❌ **JOIN 只是 SQL 关键字,不是表名,不能写 FROM Join**
- ✅ **正确写法**: `FROM enrolled e LEFT JOIN disabled d ON e.name = d.name`

> **若 pandas 在生成 CSV/PNG 时提示 `"['name'] not in index"`，表示你把 name 设为了索引。请在写入前执行 `merged_df = merged_df.reset_index(drop=True)`，或直接避免 `set_index('name')`。**

**示例：分析失业学生的性别和还款情况**

```
<Analyze>
分析失业学生的性别和还款情况
</Analyze>

<Code>
import sqlite3
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
from pathlib import Path

# 从第一轮输出中复制数据库路径
DB_PATH = r"/home/tdz/DeepAnalyze/demo/workspace/session_xxx/student_loan.sqlite"
OUTPUT_DIR = Path("generated")
OUTPUT_DIR.mkdir(parents=True, exist_ok=True)

# 合并 unemployed, male, no_payment_due 表
query = """
SELECT 
    u.name,
    CASE WHEN m.name IS NOT NULL THEN '男性' ELSE '女性' END AS gender,
    CASE WHEN n.name IS NOT NULL THEN '有还款' ELSE '无还款' END AS payment_status
FROM unemployed u
LEFT JOIN male m ON u.name = m.name
LEFT JOIN no_payment_due n ON u.name = n.name
"""

with sqlite3.connect(DB_PATH, timeout=30) as conn:
    conn.execute("PRAGMA busy_timeout = 30000;")
    df = pd.read_sql_query(query, conn)

# 统计
cross_tab = pd.crosstab(df['gender'], df['payment_status'])
cross_tab.to_csv(OUTPUT_DIR / "unemployed_gender_payment.csv", encoding='utf-8')

# 可视化
plt.figure(figsize=(8, 6))
cross_tab.plot(kind='bar', stacked=True)
plt.title("失业学生性别与还款情况")
plt.xlabel("性别")
plt.ylabel("人数")
plt.tight_layout()
plt.savefig(OUTPUT_DIR / "unemployed_gender_payment.png", dpi=120)
plt.close()

print(f"分析完成：失业学生 {len(df)} 人")
</Code>
```

### 6. 代码模板参考

**🚨 代码模板 A：读取 CSV 文件（第 2-6 轮必须使用）🚨**

```python
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
from pathlib import Path

# ⚠️ 第2-6轮必须使用CSV文件路径,不要使用DB_PATH
CSV_PATH = r"/home/tdz/DeepAnalyze/demo/workspace/session_xxx/文件名.csv"
OUTPUT_DIR = Path("generated")
OUTPUT_DIR.mkdir(parents=True, exist_ok=True)

# ✅ 必须使用 pd.read_csv() 读取CSV文件
df = pd.read_csv(CSV_PATH)

# 数据分析
summary = df.describe(include='all')
summary.to_csv(OUTPUT_DIR / "描述性文件名.csv", encoding='utf-8')

# 可视化
plt.figure(figsize=(8, 6))
sns.countplot(x='字段名', data=df)
plt.title("图表标题")
plt.tight_layout()
plt.savefig(OUTPUT_DIR / "描述性文件名.png", dpi=120)
plt.close()

print("分析完成")
```

**代码模板 B：读取 SQLite 数据库（仅第 7 轮使用）**

```python
import sqlite3
import pandas as pd
import matplotlib.pyplot as plt
from pathlib import Path

# ⚠️ 仅第7轮多表关联时使用
DB_PATH = r"真实的数据库绝对路径"
OUTPUT_DIR = Path("generated")
OUTPUT_DIR.mkdir(parents=True, exist_ok=True)

# 读取数据
with sqlite3.connect(DB_PATH, timeout=30) as conn:
    conn.execute("PRAGMA busy_timeout = 30000;")
    df = pd.read_sql_query("SELECT * FROM 表名 LIMIT 1000", conn)

# 数据分析和可视化（同上）
```

**代码模板 C：合并多个表分析（仅第 7 轮使用，基于 SQLite）**

```python
import sqlite3
import pandas as pd
from pathlib import Path

DB_PATH = r"真实的数据库绝对路径"
OUTPUT_DIR = Path("generated")
OUTPUT_DIR.mkdir(parents=True, exist_ok=True)

# 读取并去重
with sqlite3.connect(DB_PATH, timeout=30) as conn:
    conn.execute("PRAGMA busy_timeout = 30000;")
    df_enrolled = pd.read_sql_query("SELECT name, school FROM enrolled", conn).drop_duplicates(subset=["name"])
    df_no_due = (
        pd.read_sql_query("SELECT name, bool FROM no_payment_due", conn)
        .drop_duplicates(subset=["name"])
        .rename(columns={"bool": "payment_status"})
    )
    df_absense = (
        pd.read_sql_query("SELECT name, month FROM longest_absense_from_school", conn)
        .drop_duplicates(subset=["name"])
        .rename(columns={"month": "absense_month"})
    )

# 合并并保留 name 列
merged = (
    df_enrolled
    .merge(df_no_due, on="name", how="left")
    .merge(df_absense, on="name", how="left")
)

# 保证 name 仍是列（不要 set_index），避免后续选择列时报错
merged.to_csv(OUTPUT_DIR / "multi_table_join_result.csv", index=False)
print(merged.head())
```

**注意**：`male` 表只有 `name` 字段，如果 name 存在则表示是男性，不存在则为女性。

### 4. 遇到错误怎么办

**如果代码执行出错，直接修改代码，不要在 `<Analyze>` 中解释错误原因。**

常见错误处理：
- **列名不存在**（`no such column: xxx`）→ 回到第一轮结果，使用正确的列名
- **文件读取错误**（`FileNotFoundError`）→ 使用数据文件清单中的文件名
- **SQL 语法错误** → 检查表名、字段名拼写

**重要**：
- 第一轮已提供完整的表结构，不要重复查询
- 每轮只做一件事，不要一次合并多个表

**容错机制（关键）**：
- ⚠️ **如果某轮代码反复失败（如第 7 轮多表关联），尝试 2 次后必须继续下一轮**
- ✅ **在 README 中说明哪些分析成功、哪些失败即可**
- ❌ **禁止因为某轮失败就停止整个流程**

### 7. 第 8 轮：生成 README.md 索引文件（旧模板已废弃）
旧版 README 模板与当前规则冲突，已停用。请严格使用本文后部“第 8 轮：遍历 generated 目录并生成 README.md”的硬性要求与标准脚本。

### 8. 第 9 轮：生成 comprehensive_analysis_report.md（综合数据分析报告）
- **任务**：基于 `generated/` 目录下已生成的 CSV/PNG/README 等成果，读取真实数据做交叉分析，输出深度分析报告 `comprehensive_analysis_report.md`。
- **约束**：允许使用 `pathlib`、`pandas`、`datetime` 等库；必须通过 Python 构造 `markdown_lines` 并写盘，禁止直接粘贴 Markdown 文本；禁止访问原始 `data/` 目录或 SQLite。
- **说明**：请以本文后部"第 9 轮：生成综合数据分析报告"的完整模板为准。

### 9. 第 10 轮：输出最终答案

**前置条件**：已完成第 2-9 轮的所有分析任务（包含 README 与 Markdown 报告）。
**格式**：

```
<Answer>
总结你的发现：
1. 数据概况（多少表、多少记录）
2. 关键发现（至少 2 条，引用具体的执行结果）
3. 建议（下一步可以做什么）
</Answer>
```

## ⛔ 绝对禁止

**如果你违反以下规则，系统会强制终止任务：**

1. ⛔ **禁止在第 2-9 轮输出 `<Answer>` 标签**
2. ⛔ **禁止跳过任何轮次**
3. ⛔ **禁止在 `</Code>` 后继续输出任何内容**
4. ⛔ **禁止臆测字段名** - 必须先用 `PRAGMA table_info(表名)` 查询真实字段
5. ⛔ **禁止使用不存在的API参数** - 例如 `pd.merge()` 没有 `fill_value` 参数,应使用 `.fillna(0)`
6. ⛔ **禁止假设表结构** - 如果字段不存在,必须跳过该表的分析并输出警告信息

## 开始分析

**⚠️ 强制执行流程 - 必须严格按照以下顺序执行，禁止跳过或调整顺序 ⚠️**

### 第 2 轮（当前轮次）：分析 enrolled 表
**任务**：分析 `enrolled` 表的学校分布（**严禁使用 SQLite，必须走 `pd.read_csv()`**）
**表名**：`enrolled`
**字段**：name, school, month
**输出文件**：
- `enrolled_summary.csv`（必须写入 `generated/`，文件名不可改）
- `enrolled_school_dist.png`（必须写入 `generated/`，文件名不可改）

**完成标准**：
- ✅ 生成 `enrolled_summary.csv`
- ✅ 生成 `enrolled_school_dist.png`
- ✅ **完成后立即进入第 3 轮,分析 no_payment_due 表**
- ❌ **禁止继续分析 enrolled 表的其他维度**

### 第 3 轮：分析 no_payment_due 表
**任务**：分析 `no_payment_due` 表的还款状态分布（**只允许 CSV 读取，不得连 SQLite**）
**数据结构必读**：
- `no_payment_due.csv` 只有 `name` 与 `bool` 两列，`bool` 取值 `pos/neg` 表示是否暂缓还款
- 进入代码后必须将 `bool` 映射为更语义化的列，如 `df["payment_status"] = df["bool"].map({"pos": "no_payment_due", "neg": "payment_due"})`
- 生成图表时务必引用该新列（如 `payment_status`），禁止直接使用不存在的 `payment_status` 原始列名

**产出要求**：
- `payment_status_summary.csv`：输出各状态计数/占比，可保留原 `bool` 列同时加上映射列
- `payment_status_dist.png`：使用映射后的类别列绘制计数图，图标题需说明 “No Payment Due 状态分布”
- 记得 `plt.savefig(... 'payment_status_dist.png')` 并 `plt.close()`

**完成标准**：
- ✅ 生成 payment_status_summary.csv
- ✅ 生成 payment_status_dist.png
- ✅ **完成后立即进入第 4 轮,分析 longest_absense_from_school 表**

### 第 4 轮：分析 longest_absense_from_school 表
**任务**：分析 `longest_absense_from_school` 表的缺勤时长分布（**严禁 SQLite**）
**数据结构必读**：
- CSV 仅有 `name` 与 `month` 两列，`month` 表示最长缺勤月数
- 若需要 `absense`/`absense_month` 这样的语义字段，请显式派生：`df["absense_month"] = df["month"]` 或 `rename(columns={"month": "absense_month"})`
- 禁止直接访问不存在的 `absense` 列；所有统计与绘图都应基于 `month` 或派生列
- 生成描述性统计时，可使用 `summary = df[["absense_month"]].describe().rename_axis("metric").reset_index()` 并 `summary.to_csv(...)`；不要手动硬编码 8 个列名，保留 describe 默认的 `count/mean/std/...` 顺序以避免列数不匹配

**产出要求**：
- `absense_summary.csv`：至少包含对 `month`（或映射列）的描述性统计，并写入 CSV
- `absense_month_dist.png`：使用派生列绘制频数图，标题需说明 “Longest Absense Month Distribution”
- 仍需 `plt.savefig(... 'absense_month_dist.png')` 与 `plt.close()`

**完成标准**：
- ✅ 生成 absense_summary.csv
- ✅ 生成 absense_month_dist.png
- ✅ 生成 enlist_organ_dist.png
- ✅ **完成后立即进入第 6 轮,分析 disabled 表**

### 第 6 轮：分析 disabled 表
**任务**：统计 `disabled` 表的残疾学生数量（**依旧只能读 CSV，不可引用 SQLite**）
**数据结构必读**：
- `disabled.csv` 只有一列 `name`，列出所有被判定为残疾的学生；没有 `disabled` 或 `flag` 列
- 进入代码后需自行构造布尔列，例如 `df["disabled_flag"] = 1`，或与 `enrolled` 合并计算总人数
- 禁止访问不存在的列（如 `df["disabled"]`），否则会触发 `KeyError`
- 绘制 `disabled_vs_total.png` 时务必保证 `plt.xticks` 的刻度数量与标签数量一致，可使用 `bars = plt.bar(... )` 后通过 `positions = [bar.get_x() + bar.get_width() / 2 for bar in bars]` 再调用 `plt.xticks(positions, labels)`

**产出要求**：
- `disabled_count.csv`：必须至少包含 “Disabled” 与 “Total” 两行（或中文对应描述），写明残疾学生数量与总人数，可额外附带占比或布尔列。建议直接构造两列表 `metric/value` 表（如 disabled_count/total_count 对应人数），避免对 `describe()/transpose()` 结果强制改列名导致列数不匹配。
- `disabled_vs_total.png`：条形图需同时展示 “Disabled” 与 “Total” 两根柱，标题/图例清晰对比两者，保存前务必调用 `plt.tight_layout()`、`plt.savefig(... 'disabled_vs_total.png')`、`plt.close()`。
- **严格禁止**在本轮生成任何 `*_field_types.txt` 等临时文件，`generated/` 只能出现要求的 CSV 与 PNG。
- 仍需在脚本末尾 `plt.savefig(... 'disabled_vs_total.png')` 并 `plt.close()`

---

### 第 8 轮：遍历 generated 目录并生成 README.md
**任务**：扫描 `generated/` 目录，按要求输出索引文档，禁止执行 SQL / pandas / matplotlib / seaborn 等与文件遍历无关的操作。

**输出文件**：`README.md`

**硬性要求**：
1. 只能导入 `pathlib`（可选 `datetime`、`textwrap`），严禁导入 pandas/matplotlib。  
2. Markdown 必须以 `# 生成文件目录` 开头，并依次包含 `## Markdown 报告`、`## CSV 数据文件`、`## PNG 可视化`、`## 执行日志`、`## 其他文件` 五个小节。  
3. “执行日志”小节必须列出 **全部 `execute_round_*.txt`**（含 `execute_round_0_bootstrap.txt`），且 README.md 本身必须在“其他文件”中列出。  
4. 必须动态统计 `generated/` 下所有文件（不含子目录），并严格使用中文格式：`共生成 X 个文件，全部存放于 \`generated/\` 目录。` **不得加粗数字/加前缀/换行**，禁止写 `A total of ... files` 或 `本项目共生成 **X** 个文件`。  
5. 列表项必须严格使用 `- ` 开头，并带反引号与大小：``- `文件名.ext` (123 bytes)``，不允许省略 size，也不允许仅列出文件名。若某类别为空，允许使用 “- （无 XXX 文件）” 或 “- 无”。  
6. 不允许在 `<Code>` 中粘贴完整 Markdown 字符串或使用三引号固定内容，**必须遍历目录**构造 `lines` 列表，再 `write_text`。  
7. **禁止手写文件列表/字典/假数据**（如 `table_to_file = {...}`），必须基于 `files` 动态生成各分类列表；如某类别没有文件，使用“- （无 XXX 文件）”或“- 无”占位。  
8. README 内禁止出现与实际文件不符的描述（例如写成“所有文件来自第7轮”或列出不存在的 CSV/PNG）。  
9. 写盘后需 `print(f"✅ README.md 已写入...")` 方便日志确认。

**标准脚本（必须复用，可微调变量名/描述）**：
```python
from pathlib import Path
from datetime import datetime

generated_dir = Path("generated")
if not generated_dir.exists():
    raise FileNotFoundError(f"目录不存在：{generated_dir.resolve()}")

readme_path = generated_dir / "README.md"
if not readme_path.exists():
    readme_path.touch()

files = sorted([p for p in generated_dir.iterdir() if p.is_file()])
total_files = len(files)

md_report_files = [f for f in files if f.suffix.lower() == ".md" and f.name.lower() != "readme.md"]
csv_files = [f for f in files if f.suffix.lower() == ".csv"]
png_files = [f for f in files if f.suffix.lower() == ".png"]
log_files = [f for f in files if f.name.startswith("execute_round_")]
other_files = [
    f
    for f in files
    if f not in md_report_files + csv_files + png_files and f not in log_files
    and f.name.lower() != "readme.md"
]

def format_files(items):
    return [f"- `{f.name}` ({f.stat().st_size} bytes)" for f in items]

lines = [
    "# 生成文件目录",
    f"共生成 {total_files} 个文件，全部存放于 `generated/` 目录。",
    f"生成时间：{datetime.now():%Y-%m-%d %H:%M:%S}",
    "",
    "## Markdown 报告",
]
lines += format_files(md_report_files) or ["- （无 Markdown 报告）"]

lines.append("")
lines.append("## CSV 数据文件")
lines += format_files(csv_files) or ["- （无 CSV 数据文件）"]

lines.append("")
lines.append("## PNG 可视化")
lines += format_files(png_files) or ["- （无 PNG 可视化）"]

lines.append("")
lines.append("## 执行日志")
lines += format_files(log_files) or ["- （无 execute_round_*.txt）"]

lines.append("")
lines.append("## 其他文件")
lines += format_files(other_files) or ["- （无 其他文件）"]

readme_path.write_text("\n".join(lines), encoding="utf-8")
print(f"✅ README.md 已写入，列出了 {total_files} 个文件。")
```
> **严禁**用三引号定义 `markdown_content = """..."""`；如检测到将被立即退票。

---

### 第 9 轮：生成综合数据分析报告（comprehensive_analysis_report.md）
**任务**：基于 `generated/` 目录下已生成的 CSV/PNG/README 等成果，读取真实数据做**交叉分析**，输出深度分析报告 `comprehensive_analysis_report.md`。

**输出文件**：`comprehensive_analysis_report.md`

**核心约束**：
1. **禁止访问原始数据**：不得使用 `sqlite3.connect()`、不得读取 `data/` 目录下的原始 CSV 文件
2. **只能读取 generated/ 目录**：允许使用 `pandas.read_csv()` 读取 `generated/` 下的 CSV 文件
3. **允许使用的库**：`pathlib`、`pandas`、`datetime` 等
4. **输出文件**：`comprehensive_analysis_report.md`（必须写入 `generated/` 目录）

**报告结构要求（必须包含以下段落）**：
1. **数据概况**（轻量铺垫）：样本量、列名、缺失率概览
2. **交叉分析**（核心价值，至少 2 组交叉）：
   - 学校 × 缴费状态：哪些学校的欠费率最高？用 `pd.crosstab()` 或 `groupby` 计算
   - 缺勤月数 × 参军状态：参军学生的缺勤是否更长？用 `groupby("organ").agg({"month": "mean"})` 等
   - 残疾学生的学校分布是否集中？
3. **统计发现**（至少 3 条有数据支撑的洞察）：
   - 每条洞察必须引用真实列名并给出计算得到的数值（如 TopK 类别占比、均值/中位数、缺失率等）
   - 禁止泛泛而谈，必须有具体数字
4. **可视化解读**：嵌入 PNG 图表并对每张图给出一句话结论（不只是贴图）
5. **综合结论与建议**：基于以上分析给出 2-3 条可操作建议

**可追溯性要求（必须遵守）**：
- **必须写入"数据证据区块"**：明确写出读取的 CSV 文件名、`len(df)` 行数、`df.columns.tolist()` 真实列名
- 禁止占位符残留：Markdown 中不得出现 `{rows}`、`{cols}` 等未渲染模板占位符
- 禁止不可读对象字符串：写入前将标量转换为普通 Python 数值（`int(val)`/`float(val)`）
- **禁止"默认 0"**：某项无法解析/不存在时，请输出 `N/A`
- **禁止混入对话标签**：`markdown_lines` 中不要出现 `<Code>`、`</Code>` 等标签

**硬性要求**：
1. 必须先读取 `multi_table_join_result.csv`，查看其实际列名和数据
2. 禁止引用不存在的文件，禁止编造字段名
3. 必须使用 Python 构造 `markdown_lines` 列表并写入文件，禁止直接粘贴 Markdown 字符串
4. 结束时打印 "✅ comprehensive_analysis_report.md 已写入"

**推荐模板**：
```python
from pathlib import Path
from datetime import datetime
import pandas as pd

generated_dir = Path("generated")
csv_files = sorted(generated_dir.glob("*.csv"))
png_files = sorted(generated_dir.glob("*.png"))

# ── 读取主数据 ──
df = pd.read_csv(generated_dir / "multi_table_join_result.csv")
print(f"✅ 读取 multi_table_join_result.csv，共 {len(df)} 行，列名：{list(df.columns)}")

def read_csv_safe(path: Path) -> pd.DataFrame:
    if path.exists():
        return pd.read_csv(path)
    return pd.DataFrame()

df_disabled = read_csv_safe(generated_dir / "disabled_count.csv")

total = len(df)
cols = list(df.columns)

# ── 交叉分析 ──
# 1) 学校 × 缴费状态
cross_school_pay = pd.DataFrame()
if {"school", "bool"}.issubset(df.columns):
    cross_school_pay = pd.crosstab(df["school"], df["bool"], margins=True)

# 2) 参军状态 × 缺勤月数
organ_absense = pd.DataFrame()
if {"organ", "month"}.issubset(df.columns):
    organ_absense = df.groupby("organ")["month"].agg(["mean", "median", "count"])
    organ_absense = organ_absense.sort_values("mean", ascending=False)

# 3) 各列基础统计
col_stats = []
for c in cols:
    nuniq = int(df[c].nunique())
    missing = int(df[c].isnull().sum())
    miss_pct = f"{missing / total * 100:.1f}%"
    col_stats.append((c, nuniq, missing, miss_pct))

# ── 构造 Markdown ──
md = []
md.append("# Student Loan 综合数据分析报告")
md.append("")
md.append(f"生成时间：{datetime.now():%Y-%m-%d %H:%M:%S}")
md.append("")

# 1. 数据概况
md.append("## 1. 数据概况")
md.append(f"- **数据来源**：`multi_table_join_result.csv`（{total} 行，{len(cols)} 列）")
md.append(f"- **字段列表**：{', '.join(cols)}")
md.append(f"- **CSV 文件数**：{len(csv_files)}，**PNG 图表数**：{len(png_files)}")
md.append("")
md.append("| 字段 | 唯一值 | 缺失数 | 缺失率 |")
md.append("|------|--------|--------|--------|")
for c, nuniq, missing, miss_pct in col_stats:
    md.append(f"| {c} | {nuniq} | {missing} | {miss_pct} |")
md.append("")

# 2. 交叉分析
md.append("## 2. 交叉分析")
md.append("")
if not cross_school_pay.empty:
    md.append("### 2.1 学校 × 缴费状态")
    md.append("以下为各学校的缴费状态交叉统计：")
    md.append("")
    md.append(cross_school_pay.to_markdown())
    md.append("")
    # 找出欠费率最高的学校
    if "neg" in cross_school_pay.columns and "All" in cross_school_pay.columns:
        rates = (cross_school_pay["neg"] / cross_school_pay["All"]).drop("All", errors="ignore")
        top_school = rates.idxmax()
        top_rate = f"{rates.max() * 100:.1f}%"
        md.append(f"**发现**：欠费率最高的学校为 **{top_school}**（{top_rate}）。")
    md.append("")

if not organ_absense.empty:
    md.append("### 2.2 参军机构 × 缺勤月数")
    md.append("")
    md.append("| 机构 | 平均缺勤月数 | 中位数 | 人数 |")
    md.append("|------|-------------|--------|------|")
    for org, row in organ_absense.iterrows():
        md.append(f"| {org} | {row['mean']:.1f} | {row['median']:.1f} | {int(row['count'])} |")
    md.append("")
    top_org = organ_absense.index[0]
    top_mean = f"{organ_absense.iloc[0]['mean']:.1f}"
    md.append(f"**发现**：平均缺勤月数最长的机构为 **{top_org}**（{top_mean} 个月）。")
    md.append("")

# 3. 统计发现
md.append("## 3. 统计发现")
md.append("")
# 类别列 TopK
for c in cols:
    if df[c].dtype == "object":
        vc = df[c].value_counts()
        top3 = vc.head(3)
        items = "; ".join(f"{k}={int(v)}" for k, v in top3.items())
        md.append(f"- **{c}** 共 {int(vc.shape[0])} 个类别，Top3：{items}")
# 数值列统计
for c in cols:
    if pd.api.types.is_numeric_dtype(df[c]):
        mean_v = float(df[c].mean())
        med_v = float(df[c].median())
        md.append(f"- **{c}** 均值={mean_v:.2f}，中位数={med_v:.2f}，"
                  f"Q25={float(df[c].quantile(0.25)):.2f}，Q75={float(df[c].quantile(0.75)):.2f}")
md.append("")

# 4. 可视化解读
md.append("## 4. 可视化图表解读")
md.append("")
chart_desc = {
    "enrolled_school_dist.png": "展示各学校的学生注册人数分布，可识别生源集中度",
    "payment_status_dist.png": "展示缴费/欠费状态占比，反映整体还款健康度",
    "absense_month_dist.png": "展示最长缺勤月数的频率分布，识别缺勤集中区间",
    "enlist_organ_dist.png": "展示参军学生的机构分布，了解兵源结构",
    "disabled_vs_total.png": "对比残疾学生与总人数，量化特殊群体占比",
    "multi_table_join_result.png": "多表关联结果的综合可视化",
}
for png in sorted(png_files):
    desc = chart_desc.get(png.name, "数据可视化图表")
    md.append(f"### {png.stem}")
    md.append(f"![{png.stem}]({png.name})")
    md.append(f"> {desc}")
    md.append("")

# 5. 结论与建议
md.append("## 5. 结论与建议")
md.append("")
md.append(f"1. **生源分布不均**：{int(df['school'].nunique()) if 'school' in df.columns else 'N/A'} 所学校中，"
          "部分学校学生集中度较高，建议针对重点学校制定差异化贷款审批策略。")
md.append(f"2. **缺勤风险**：缺勤月数中位数为 {float(df['month'].median()) if 'month' in df.columns else 'N/A'}，"
          "长期缺勤学生可能面临学业中断风险，需关注其还款能力。")
md.append("3. **特殊群体关注**：参军学生和残疾学生属于政策优惠对象，"
          "建议在贷款审批流程中增加专项通道。")
md.append("")

report_path = generated_dir / "comprehensive_analysis_report.md"
report_path.write_text("\n".join(md), encoding="utf-8")
print(f"✅ comprehensive_analysis_report.md 已写入，共 {len(md)} 行。")
```
> 如需增加更多段落，请继续在 `md` 列表中 append，而不是粘贴整段 Markdown。

---

### 第 10 轮：输出总结报告
**任务**：仅输出一个 `<Answer>`，形成对本次 9 轮（2-9 轮）分析与产出的总结报告。

**写作要求（务必遵守）**：  
1. 仅允许输出 `<Answer>`，禁止再写 `<Analyze>` / `<Code>`。  
2. 报告需回答三件事：  
   - **做了哪些分析**：逐轮概述第 2-9 轮的任务与方法。  
   - **生成了哪些结果**：列出关键文件（CSV/PNG/README/MD 等）并点明它们代表的含义或可视化内容。  
   - **整体结论/下一步**：基于已生成的文件给出至少 2 条定量或定性结论（务必引用具体文件作为依据），如出现被回滚的轮次也需说明原因及修复。  
3. `<Answer>` 内正文可以是 Markdown 段落，但不得引用外部脚本或 CSS。  
4. **禁止编造文件名**：只能引用以下真实存在的文件：
   - CSV: `enrolled_summary.csv`, `payment_status_summary.csv`, `absense_summary.csv`, `enlist_summary.csv`, `disabled_count.csv`, `multi_table_join_result.csv`
   - PNG: `enrolled_school_dist.png`, `payment_status_dist.png`, `absense_month_dist.png`, `enlist_organ_dist.png`, `disabled_vs_total.png`, `multi_table_join_result.png`
   - MD: `comprehensive_analysis_report.md`
   - 其他: `README.md`
5. **禁止编造数据内容**：不得提及不存在的字段（如 `loan_amount`、`monthly_income`、`region` 等），只能基于实际的学生数据字段（name, school, bool, month, organ 等）进行总结。  

---

## 🔒 轮次进度追踪

**当前进度**：第 2 轮 / 共 10 轮
**已完成**：第 1 轮（Bootstrap）
**待完成**：第 2, 3, 4, 5, 6, 7, 8, 9, 10 轮

**⚠️ 重要提醒**：
- 每轮输出 `</Code>` 后立即停止
- 等待代码执行完成后，立即开始下一轮
- 不要等待用户指令，不要询问用户需求
- **第 8 轮：生成 README.md（保持不变）**
- **第 9 轮：生成 comprehensive_analysis_report.md（综合数据分析报告）**
- **第 10 轮：输出 `<Answer>` 总结报告**
