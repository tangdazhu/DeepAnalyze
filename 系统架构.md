# DeepAnalyze 系统架构（工程视角）

> 本文基于当前仓库代码梳理生成，所有关键结论均可在对应源码中追溯。

## 1. 系统整体架构与模块关系

DeepAnalyze 在本仓库中主要存在三条“可运行链路”（从最贴近当前工程实现出发）：

### 1.1 Demo WebUI 链路（推荐用于 WebUI）

- **前端**：`demo/chat/`（WebUI，调用后端接口）
- **后端**：`demo/backend.py`
- **模型服务**：外部 vLLM（OpenAI 兼容接口），由 `demo/backend.py` 通过 `openai` SDK 调用
- **会话工作区**：`demo/workspace/session_<...>/`（每个会话一个目录）

这条链路的定位是：
- 由 `demo/backend.py` 同时承担
  - `/chat/completions`（对接模型、流式输出）
  - `/workspace/*`（上传/列目录/下载等）
  - 以及本地代码执行（将模型生成的 `<Code>` 作为 Python 脚本执行）

可追溯入口：
- 后端主循环与执行：`demo/backend.py -> bot_stream(...)`
- 代码执行：`demo/backend.py -> execute_code_safe(...)`（子进程执行）
- 上传到会话目录：`demo/backend.py -> upload_to_dir(...)`（`POST /workspace/upload-to`）

### 1.2 OpenAI 兼容 API 链路（用于 CLI 或对接外部调用方）

- **API 服务入口**：`API/main.py -> create_app()` / `API/start_server.py`
- **OpenAI 兼容路由**：
  - 文件：`API/file_api.py`（`/v1/files`）
  - 模型列表：`API/models_api.py`（`/v1/models`）
  - 聊天：`API/chat_api.py`（`/v1/chat/completions`）
- **文件下载服务**：`API/utils.py -> start_http_server()`（由 `API/main.py` 启动线程，端口在 `API/config.py`）
- **会话工作区**：`workspace/thread-<...>/`（每个 thread 一个目录）

可追溯入口：
- `API/start_server.py` 仅调用 `API/main.py -> main()`
- `API/main.py` 通过 `uvicorn.run(app, ...)` 启动服务，并 `include_router(...)` 挂载各路由
- `API/chat_api.py -> chat_completions(...)` 在流式/非流式路径中，负责“模型输出 → 提取 Code → 执行 → 产物收集 → 将 <Execute>/<File> 追加到流式输出”

### 1.3 Python 直接调用链路（脚本/库形式）

- `deepanalyze.py -> class DeepAnalyzeVLLM`
- 示例：`run.py`

其定位是：
- 直接通过 HTTP 调 vLLM（默认 `api_url="http://localhost:8000/v1/chat/completions"`）
- 多轮循环解析 `<Code>` 并 `exec(...)` 执行

可追溯入口：
- `deepanalyze.py -> DeepAnalyzeVLLM.generate(...)`

> 注意：该链路使用 `exec(code_str, {})` 在当前进程执行（见 `DeepAnalyzeVLLM.execute_code`），与 demo/API 链路的“子进程执行”不同。

---

## 2. 多 Agent 设计（SkyRL / skyagent）

仓库中多 agent 相关实现主要位于：
- `DeepAnalyze/SkyRL/skyagent/skyagent/agents/`

它体现的是训练/研究框架中的 agent 抽象与执行器，和 demo/API 的“运行时 WebUI”是两条并行的体系（但可以复用工具调用思想）。

### 2.1 核心概念

1. **AgentRunner（运行器）**
- 文件：`DeepAnalyze/SkyRL/skyagent/skyagent/agents/base.py`
- 入口：`AgentRunner.from_task(task, infer_engine, tokenizer)`
  - 从任务名或 YAML 路径加载配置（`OmegaConf.load`）
  - 构造推理后端 `build_backend(...)`
  - 选择轨迹类 `traj_cls`、任务类 `task`

2. **Trajectory（轨迹）**
- 例如：`DeepAnalyze/SkyRL/skyagent/skyagent/agents/react/react_runner.py -> ReaActTrajectory`
- 负责：
  - 初始化 agent
  - 生成轨迹（循环交互）
  - 评估轨迹（reward 等）

3. **ReActAgent（工具调用式 Agent）**
- 文件：`DeepAnalyze/SkyRL/skyagent/skyagent/agents/react/react_agent.py`
- 关键方法：
  - `run(instruction)`：循环 `step()` 直到结束或达到 `max_iterations`
  - `step()`：
    - 将消息转换为模型可接受格式
    - 通过 `infer_engine.async_generate_ids(...)` 获取模型响应
    - 将响应转换为“函数调用格式”
    - 分发 tool 执行，将结果作为 `tool` role 写回 messages

### 2.2 多 Agent 的交互示例（基于源码抽象）

下面示例代码展示了 SkyRL/skyagent 体系下典型的“任务→轨迹→agent→工具调用”的流程（对应 `AgentRunner.from_task` + `ReaActTrajectory.generate_trajectory` + `ReActAgent.run`）。

```python
# 说明：示例结构来自以下源码：
# - DeepAnalyze/SkyRL/skyagent/skyagent/agents/base.py  (AgentRunner.from_task)
# - DeepAnalyze/SkyRL/skyagent/skyagent/agents/react/react_runner.py (ReaActTrajectory.generate_trajectory)
# - DeepAnalyze/SkyRL/skyagent/skyagent/agents/react/react_agent.py  (ReActAgent.run/step)

from skyagent.agents.base import AgentRunner

# infer_engine/tokenizer 的具体构造依赖 SkyRL 的 backend 与 tokenizer 初始化代码，
# 此处只展示调用关系（需按你的环境注入真实对象）。

runner = AgentRunner.from_task(
    task="path/to/task.yaml",  # 或 TASK_CONFIG_REGISTRY 中的任务名
    infer_engine=infer_engine,
    tokenizer=tokenizer,
)

# 轨迹对象一般由 runner 侧构造并注入数据，这里用“概念化”的方式展示：
trajectory = runner.traj_cls()

# 轨迹会在 generate_trajectory 内部创建 ReActAgent 并执行：
# finish_reason, result = await agent.run(instruction)
await trajectory.generate_trajectory()

# 评估（可选）
await trajectory.evaluate_trajectory()
```

> 说明：上述示例强调“模块关系与调用链”，具体 task YAML、tools 列表、infer_backend 等由 SkyRL 配置体系决定（见 `AgentRunner.__init__` 中对 `cfg.generator.*` 的读取）。

---

## 3. 系统中生成的代码如何运行？

这里分 demo/backend.py 与 API/chat_api.py 两条运行时链路说明（它们的策略相似）。

### 3.1 Demo（`demo/backend.py`）的代码执行机制

**关键点**：
- 模型输出通过 `bot_stream(...)` 流式接收
- 检测到完整 `<Code>...</Code>` 后，提取 Python 代码字符串
- 使用 `execute_code_safe(...)` 将代码写入临时 `.py` 文件并在**子进程**运行

可追溯实现：
- `demo/backend.py -> execute_code_safe(code_str, workspace_dir, timeout_sec=120)`
  - 在 `workspace_dir` 下创建临时文件：`tempfile.mkstemp(suffix=".py", dir=exec_cwd)`
  - 子进程执行：`subprocess.run([sys.executable, tmp_path], cwd=exec_cwd, capture_output=True, timeout=timeout_sec, env=child_env)`
  - 执行完成后删除临时脚本文件
  - 环境变量：设置 `MPLBACKEND=Agg` / `QT_QPA_PLATFORM=offscreen` 等（避免 GUI 后端）

**结论**：
- 这是“**子进程隔离 + 超时**”策略，能避免主进程被卡死、避免部分 GUI 问题。
- 但它不是严格意义的安全沙箱（见 3.3）。

### 3.2 API（`API/chat_api.py`）的代码执行机制

可追溯实现：
- `API/chat_api.py -> chat_completions(...)
  - `prepare_vllm_messages(...)` 注入 `# Instruction/# Data`
  - 流式时：内部 generator 持续调用 vLLM 并累积 `cur_res`
  - `extract_code_from_segment(cur_res)` 从 `<Code>` 段提取代码
  - `execute_code_safe(code_str, workspace_dir)` 执行
  - `WorkspaceTracker.diff_and_collect()` 收集新增/修改文件到 `generated/`
  - 组装并追加系统生成的 `<Execute>` 与 `<File>` 输出

相关函数：
- `API/utils.py -> extract_code_from_segment(...)`
- `API/utils.py -> execute_code_safe(...)`
- `API/utils.py -> WorkspaceTracker`（见 3.4）

### 3.3 是否需要额外的沙箱环境？

从当前实现看：
- demo/API 运行时均是用 `subprocess.run([sys.executable, tmp.py], cwd=workspace)` 执行
- 没有看到容器化、seccomp、权限隔离、网络隔离等强沙箱措施

因此：
- **不“必须”额外沙箱才能运行**（工程当前默认直接执行）
- 但从安全角度：如果要在不可信输入场景运行，建议你在部署侧提供额外隔离，例如：
  - 用 Docker/Podman 运行后端与执行器
  - 将执行进程放在受限用户权限下
  - 限制网络访问、文件系统权限、CPU/内存等

> 以上建议为部署安全通用策略；仓库当前代码层面仅实现了“子进程+超时”。

### 3.4 中间文件放在哪里？

#### Demo WebUI 会话
- 会话目录：`demo/backend.py -> get_session_workspace(session_id)`（实际目录形如 `demo/workspace/session_<...>/`）
- 生成目录：`<session_workspace>/generated/`
- 临时脚本：执行时在会话目录下生成 `tmp_*.py`，执行完删除（见 `execute_code_safe`）
- 执行日志：`generated/execute_round_<n>.txt`（`bot_stream` 在执行后写入）
- bootstrap 日志：`generated/execute_round_0_bootstrap.txt`（schema bootstrap 写入）

#### OpenAI API thread
- 会话目录：`API/utils.py -> get_thread_workspace(thread_id)`，位于 `API/config.py -> WORKSPACE_BASE_DIR`（默认 `workspace/`）
- 生成目录：`workspace/<thread_id>/generated/`

#### 额外：报告文件
- API 链路会额外写入会话报告：`API/utils.py -> generate_report_from_messages(...)`
  - 保存到：`workspace/<thread_id>/generated/Conversation_Report_<timestamp>.md`

### 3.5 如果代码需要额外依赖，如何解决？

从执行方式可知：
- demo/API 都是 `sys.executable` 执行临时脚本
- 因此“模型生成的代码”使用的依赖来源于：**运行后端服务的 Python 环境**
- 如果脚本 `import xxx` 但环境未安装，将在 `<Execute>` 中报 `ModuleNotFoundError`

推荐处理方式（按仓库现状与工程实践）：

1. **将依赖安装到推理环境中**
- 仓库文档建议使用单独的推理环境：`conda create -n deepanalyze python=3.12`（见 `README.md` / `install安装说明.md`）
- 依赖通过 `pip install -r requirements.txt` 安装（见 `install安装说明.md`）

2. **把新增依赖同步到依赖清单**
- 按你的项目规则：新增依赖应更新 `requirements.txt`（或对应子模块配置）
- 这样可以保证环境可复现

3. **避免运行时“自动安装依赖”**
- 当前执行器没有实现“自动 pip 安装”逻辑（代码中未看到相关实现）
- 生产环境不建议让模型自行 `pip install`（可复现性/安全性/供应链风险）

---

## 4. 你可以如何阅读代码（建议路线）

- 先看 WebUI 链路：
  - `demo/backend.py -> bot_stream(...)`（流式模型输出、校验、执行、产物落盘的主逻辑）
  - `demo/backend.py -> execute_code_safe(...)`（执行策略）

- 再看 OpenAI API 链路：
  - `API/main.py`（服务入口与路由挂载）
  - `API/chat_api.py`（聊天+执行+产物收集）
  - `API/utils.py`（workspace、执行器、产物追踪、报告生成）
  - `API/storage.py`（thread/workspace 的创建与清理）

- 最后看多 agent（训练/研究）链路：
  - `DeepAnalyze/SkyRL/skyagent/skyagent/agents/base.py`（AgentRunner 抽象）
  - `DeepAnalyze/SkyRL/skyagent/skyagent/agents/react/react_agent.py`（ReActAgent 的 step/run）
  - `DeepAnalyze/SkyRL/skyagent/skyagent/agents/react/react_runner.py`（轨迹生成与任务评估）

---

## 5. 版本与环境说明（可追溯）

- Python：仓库文档建议 `python=3.12`（见 `README.md` 与 `install安装说明.md`）
- vLLM：`README.md` 标注 `vllm>=0.8.5`
- API 默认端口：
  - vLLM：`8000`（见 `API/config.py -> API_BASE` 及文档）
  - API Server：`8200`（见 `API/config.py -> API_PORT`）
  - File Server：`8100`（见 `API/config.py -> HTTP_SERVER_PORT`）

