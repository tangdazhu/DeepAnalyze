# DeepAnalyze 模型选择指南

## 概述

DeepAnalyze 项目的代码生成任务对模型能力有较高要求。本文档根据不同的 GPU 显存配置，提供模型选择建议。

## GPU 显存需求分析

### 基本计算公式

- **FP16/BF16 精度**：每 1B 参数约需 2GB 显存
- **实际运行**：加上 KV cache 和其他开销，实际需求会更高

### 各模型显存需求

| 模型 | 参数量 | 理论显存 | 实际显存 | 16GB GPU | 24GB GPU | 32GB+ GPU |
|------|--------|---------|---------|----------|----------|-----------|
| qwen2.5-3b-instruct | 3B | ~6GB | ~8-10GB | ✅ 可运行 | ✅ 可运行 | ✅ 可运行 |
| qwen2.5-7b-instruct | 7B | ~14GB | ~16-18GB | ⚠️ 需优化 | ✅ 可运行 | ✅ 可运行 |
| DeepAnalyze-8B | 8B | ~16GB | ~18-20GB | ❌ 显存不足 | ✅ 可运行 | ✅ 可运行 |
| qwen2.5-14b-instruct | 14B | ~28GB | ~30-32GB | ❌ 显存不足 | ❌ 显存不足 | ✅ 可运行 |

### 量化版本显存需求（理论值）

**注意**：官方未提供 DeepAnalyze-8B 的量化版本，以下为理论估算值。

| 模型 | 4-bit 量化显存 | 8-bit 量化显存 | 官方支持 |
|------|---------------|---------------|---------|
| qwen2.5-7b-instruct | ~4-5GB | ~8-9GB | ❌ 需自行量化 |
| DeepAnalyze-8B | ~5-6GB | ~9-10GB | ❌ 需自行量化 |
| qwen2.5-14b-instruct | ~8-9GB | ~15-16GB | ❌ 需自行量化 |

## 模型能力对比

### 代码生成能力

| 模型 | 长代码生成 | 格式遵守 | 推荐度 |
|------|-----------|---------|--------|
| **DeepAnalyze-8B** | ⭐⭐⭐⭐⭐ 最强 | ⭐⭐⭐⭐⭐ 最好 | ⭐⭐⭐⭐⭐ 最推荐 |
| qwen2.5-7b-instruct | ⭐⭐⭐⭐ 强 | ⭐⭐⭐⭐ 好 | ⭐⭐⭐⭐ 推荐 |
| qwen2.5-3b-instruct | ⭐⭐ 弱 | ⭐⭐ 一般 | ⭐⭐ 不推荐 |

### 已知问题

- **qwen2.5-3b-instruct**：无法完成 30-40 行的长代码生成，在生成过程中会"卡住"，导致代码不完整且缺少 `</Code>` 标签。
- **qwen2.5-7b-instruct**：能力明显强于 3B，可以完成长代码生成，但不如 DeepAnalyze-8B 专业。
- **DeepAnalyze-8B**：专门为数据分析任务训练，表现最佳，但需要较大显存。

## 推荐方案

### 方案 1：24GB+ GPU（最佳方案）

**推荐模型**：DeepAnalyze-8B

**启动命令**：
```bash
# 下载模型
huggingface-cli download RUC-DataLab/DeepAnalyze-8B --local-dir ~/models/DeepAnalyze-8B

# 启动 vLLM
vllm serve ~/models/DeepAnalyze-8B \
  --host 0.0.0.0 --port 8000 \
  --served-model-name DeepAnalyze-8B \
  --trust-remote-code
```

**后端配置**：
```python
MODEL_PATH = "DeepAnalyze-8B"
```

### 方案 2：16GB GPU + 优化参数（❌ 已确认无法运行）

**推荐模型**：qwen2.5-7b-instruct

**⚠️ 重要提示**：经过多次实测，即使使用最激进的优化参数，7B 模型在 16GB GPU 上仍然无法运行。

**实测结果**：
- 模型加载成功：占用 14.25 GiB
- KV cache 显存：-2.38 GiB（负数，说明显存不足）
- 错误信息：`ValueError: No available memory for the cache blocks`

**结论**：16GB GPU 无法运行 7B 模型，请使用方案 4（3B 模型）。

**尝试启动命令**（可能失败）：
```bash
# 下载模型
huggingface-cli download Qwen/Qwen2.5-7B-Instruct --local-dir ~/models/qwen2.5-7b-instruct

# 启动 vLLM（使用更激进的优化参数）
vllm serve ~/models/qwen2.5-7b-instruct \
  --host 0.0.0.0 --port 8000 \
  --served-model-name qwen2.5-7b-instruct \
  --trust-remote-code \
  --max-model-len 2048 \
  --gpu-memory-utilization 0.85 \
  --dtype half \
  --enforce-eager
```

**参数说明**：
- `--max-model-len 2048`：限制最大序列长度为 2048（降低 KV cache 显存）
- `--gpu-memory-utilization 0.85`：使用 85% GPU 显存（留出更多余量）
- `--dtype half`：使用 FP16 精度
- `--enforce-eager`：禁用 CUDA graph，节省显存

**后端配置**：
```python
MODEL_PATH = "qwen2.5-7b-instruct"
```

**结论**：不推荐在 16GB GPU 上使用 7B 模型，建议使用方案 4（3B 模型）或升级硬件。

### 方案 3：16GB GPU + 自行量化（高级方案）

**推荐模型**：自行量化 DeepAnalyze-8B

**注意**：官方未提供 AWQ/GPTQ 量化版本，需要自行使用 AutoAWQ 或 AutoGPTQ 工具进行量化。量化过程较复杂，需要一定技术能力。

**量化步骤**（参考）：
```bash
# 使用 AutoAWQ 量化（需要先安装 autoawq）
pip install autoawq

# 运行量化脚本（需要自行编写或参考 AutoAWQ 文档）
python quantize_awq.py --model ~/models/DeepAnalyze-8B --output ~/models/DeepAnalyze-8B-AWQ
```

**不推荐原因**：量化过程复杂，且可能影响模型性能。建议直接使用方案 2（7B 模型）。

### 方案 4：16GB GPU（实际推荐方案）

**推荐模型**：qwen2.5-3b-instruct

**⚠️ 重要**：经过实测，16GB GPU 无法运行 7B 模型（会 OOM）。因此，**3B 模型是 16GB GPU 的唯一可行选择**。

**启动命令**：
```bash
# 下载模型（如果还没有）
huggingface-cli download Qwen/Qwen2.5-3B-Instruct --local-dir ~/models/qwen2.5-3b-instruct

# 启动 vLLM
vllm serve ~/models/qwen2.5-3b-instruct \
  --host 0.0.0.0 --port 8000 \
  --served-model-name qwen2.5-3b-instruct \
  --trust-remote-code
```

**后端配置**：
```python
MODEL_PATH = "qwen2.5-3b-instruct"
```

**已知限制**：
- 3B 模型能力有限，可能无法完成 30-40 行的长代码生成
- 代码生成过程中可能"卡住"，导致输出不完整
- 建议升级到 24GB+ GPU 以使用 DeepAnalyze-8B 获得最佳体验

**缓解措施**：
- 降低 `temperature` 参数（已设置为 0.1）
- 简化任务复杂度
- 分步生成代码

## 配置步骤

### 1. 更新后端配置

编辑 `demo/backend.py`，修改 `MODEL_PATH`：

```python
# 根据你选择的方案，修改为对应的模型名称
MODEL_PATH = "DeepAnalyze-8B"  # 或 "qwen2.5-7b-instruct"
```

### 2. 重启服务

```bash
# 停止当前 vLLM 服务（Ctrl+C）

# 启动新的 vLLM 服务（使用上面对应方案的命令）

# 重启后端服务
cd ~/DeepAnalyze/demo
python backend.py
```

### 3. 测试验证

在浏览器中刷新页面，上传数据并测试：
- ✅ 模型是否能完整生成代码
- ✅ 是否正确输出 `</Code>` 标签
- ✅ 后端是否成功执行代码
- ✅ 是否生成 CSV 和 PNG 文件

## 故障排查

### 问题 1：显存不足（OOM）

**症状**：vLLM 启动失败或运行中崩溃，提示 "CUDA out of memory"

**解决方案**：
1. 降低 `--max-model-len`（如 4096 → 2048）
2. 降低 `--gpu-memory-utilization`（如 0.9 → 0.8）
3. 使用更小的模型或量化版本

### 问题 2：代码生成不完整

**症状**：模型输出代码后停止，缺少 `</Code>` 标签

**可能原因**：
1. 模型能力不足（3B 模型）
2. 序列长度限制过小
3. 模型生成超时

**解决方案**：
1. 升级到 7B 或 8B 模型
2. 增加 `--max-model-len`
3. 检查 vLLM 日志是否有错误

### 问题 3：生成速度慢

**症状**：模型生成速度很慢，每个 token 需要几秒

**可能原因**：
1. GPU 利用率不足
2. 批处理大小过小
3. 模型加载到 CPU 而非 GPU

**解决方案**：
1. 检查 `nvidia-smi` 确认 GPU 使用情况
2. 调整 vLLM 参数（如 `--max-num-seqs`）
3. 确认模型正确加载到 GPU

## 参考资源

- [DeepAnalyze GitHub](https://github.com/ruc-datalab/DeepAnalyze)
- [DeepAnalyze-8B 模型](https://huggingface.co/RUC-DataLab/DeepAnalyze-8B)
- [vLLM 文档](https://docs.vllm.ai/)
- [Qwen2.5 模型](https://huggingface.co/Qwen)

## 更新日志

- **2024-12-11**：创建文档，添加 16GB GPU 优化方案
- **2024-12-11**：确认官方未提供 AWQ/GPTQ 量化版本，更新方案 3 说明
- **2024-12-12**：实测确认 7B 模型在 16GB GPU 上 OOM，更新方案 2 和方案 4
