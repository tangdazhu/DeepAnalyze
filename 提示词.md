请基于 workspace 中 example/analysis_on_student_loan/student_loan.sqlite（学生贷款申请与在校状态相关数据）自主完成端到端的数据探索、可视化与报告撰写。严禁捏造表/字段；所有结论都需引用真实查询或图表。

执行规范（逐条严格遵守）：

1. 交互节奏
   1.1 始终使用 <Analyze> → <Code> → <Execute> → <File> → <Answer>。
       【重要】：每轮必须同时包含 <Analyze> 和 <Code> 两个标签，缺一不可。连续 3 轮未输出 <Code> 标签将导致任务强制终止。
   
   1.2 首轮 <Analyze> 必须写明"当前目标=列出数据库实际表结构"，同轮 <Code> 只执行 `SELECT name, sql FROM sqlite_master ...`，系统会自动执行代码并在下一轮消息中注入 <Execute> 结果.
       【注意】：若模型未输出首轮 <Analyze>，系统会自动执行 schema bootstrap 并注入结果，模型应直接基于该结果进入第 2 轮分析。
       【重要】：你不能在自己的输出中包含 <Execute> 标签，<Execute> 只能由系统在执行代码后自动生成。
   
   1.3 自第二轮起，<Analyze> 必须引用上一轮 <Execute> 的表/字段或图表，并说明"根据第 X 轮 sqlite_master/PRAGMA/查询输出，准备分析表 Y 的字段 A/B…"，不得出现"继续""如前所述"等空洞词。
       【重要】：每轮 <Analyze> 的内容必须不同，不得与上一轮完全相同。若连续输出相同的 <Analyze>，系统将判定为无效并提示重写。
   
   1.4 【严禁编造字段】：只能使用首轮 <Execute> 输出的【表字段详情】中明确列出的字段。例如，若 person 表只有 name 字段，则不能在 <Analyze> 或 <Code> 中引用 age、gender 等不存在的字段。若引用了不存在的字段，代码执行时会报错，系统将提示重写。

2. 阶段目标
   - 第 2 轮：任选核心表（如 person、enrolled）做行数/缺失值/基础指标概览。
   - 第 3 轮起：至少完成一次聚合或可视化，并同时满足：① 将任一统计结果写入 `generated/<purpose>.csv`；② 保存至少一个图表到 `generated/<purpose>.png`。请在 <Analyze> 显式说明计划生成的文件名。
   - 如需重新查询 schema，必须在 <Analyze> 解释原因（例如字段缺失），避免无意义重复。

3. 代码规范
   - 每个 <Code> 都是完整可运行脚本，包含 `import sqlite3`，若使用 pandas/matplotlib/seaborn 须显式 `import pandas as pd` / `import matplotlib.pyplot as plt` / `import seaborn as sns`。
   - 只能连接 workspace 中真实存在的数据库文件，**必须从首轮 <Execute> 后的分隔线内复制系统提示的绝对路径**。**严禁使用相对路径**（如 `student_loan.sqlite` 或 `example/...`）**或自行编造路径**，否则代码执行时无法找到数据库文件。连接后第一时间执行 `conn.execute("PRAGMA busy_timeout = 30000;")`，读完数据再 `conn.close()`，禁止 `CREATE/DROP/ALTER TABLE`。
   - 生成图表必须调用 `plt.savefig('generated/<purpose>.png', dpi=120)` 并紧接 `plt.close()`；所有统计/表格结果需使用 `DataFrame.to_csv('generated/<purpose>.csv', index=False, encoding='utf-8')` 落盘；若本轮 <Code> 未包含任何写盘语句（CSV 或 PNG），系统将判定本轮无效并提示重写。
   
   【第 2 轮起每个 <Code> 必须遵循以下完整结构】：
   
   import os
   from pathlib import Path
   import sqlite3
   import pandas as pd
   import matplotlib.pyplot as plt
   import seaborn as sns

   # 【关键】从首轮 <Execute> 后的系统提示中复制数据库绝对路径
   # 系统会在分隔线内明确标注：【数据库绝对路径】请在后续所有代码中使用以下路径
   # 禁止使用相对路径（如 r"student_loan.sqlite"），否则执行失败
   DB_PATH = r"<从首轮输出中复制完整路径>"
   OUTPUT_DIR = Path("generated")
   OUTPUT_DIR.mkdir(parents=True, exist_ok=True)

   # 1. 读取数据（使用 timeout 避免锁）
   with sqlite3.connect(DB_PATH, timeout=30) as conn:
       conn.execute("PRAGMA busy_timeout = 30000;")
       df = pd.read_sql_query("SELECT * FROM <表名> LIMIT 1000", conn)

   # 2. 统计并保存 CSV
   summary = df.describe(include="all").transpose().reset_index()
   summary.to_csv(OUTPUT_DIR / "<表名>_summary.csv", index=False, encoding="utf-8")

   # 3. 可视化并保存 PNG
   plt.figure(figsize=(6, 4))
   sns.countplot(x="<字段名>", data=df)
   plt.title("<表名> <字段名> distribution")
   plt.tight_layout()
   plt.savefig(OUTPUT_DIR / "<表名>_<字段名>_dist.png", dpi=120)
   plt.close()

   print("Summary rows:", len(summary))
   print("Sample data:\n", df.head())
   
   【重要】：请在 <Analyze> 中明确声明本轮要生成的文件名（如 "person_summary.csv" 和 "person_gender_dist.png"），并在 <Code> 中严格按上述结构执行，确保每轮都有真实产物。若连续 3 轮未输出有效代码，系统将自动终止任务。

4. 错误溯源与执行结果
   <Execute> 报错或为空时，下一轮 <Analyze> 必须引用具体错误信息，解释原因并提出修复方案后再执行。
   【严禁伪造 <Execute> 标签】：`<Execute>` 块只能由系统在执行你的代码后自动生成，包含真实的执行输出、错误信息或数据预览。你不能在 <Analyze> 或 <Code> 之外输出任何 <Execute> 内容，也不能伪造执行日志（如 `[INFO] [Execution] Code execution completed successfully`）、字段详情、可视化结果等。若检测到你在输出中包含 <Execute> 标签但没有提供 <Code> 标签，系统将立即拦截并提示重写。

5. 文件声明
   【严禁伪造 <File> 标签】：只允许系统根据真实落盘文件自动生成 `<File>` 段，内容形如 `- [enrolled_gender_distribution.png](...)`；若无产物写"暂无文件"。禁止在模型输出中自行撰写 `<File>` 内容或伪造链接。

6. 结论要求
   - 至少完成两次非 schema 执行后才能输出 <Answer>。
   - 所有结论需引用来源（例如"根据第 3 轮 <Execute> 的 SELECT 结果，person 表共有 500 条记录"）。
   - <Answer> 至少给出两条定量发现和一条后续建议，并指明对应的 <Execute> 或 <File>。
   - 【HTML 报告生成】：在输出 <Answer> 之前，必须先生成一个完整的 HTML 分析报告（`generated/analysis_report.html`），报告需包含：
     * 数据概览：数据库名称、表数量、总记录数
     * 各表分析：每个表的基本统计信息、字段分布
     * 可视化图表：嵌入所有生成的 PNG 图表（使用相对路径）
     * 关键发现：列出 2-3 条定量发现
     * 建议：给出后续分析建议
     * 报告需使用现代 HTML/CSS 样式，确保可读性和美观性

7. 重复 schema 防护
   schema 已确认后，若再次输出仅含 sqlite_master 查询且未说明必要原因，系统将终止任务并返回错误。

从"列出数据库实际表结构"开始行动，逐轮遵守以上约束并保持引用可追溯。