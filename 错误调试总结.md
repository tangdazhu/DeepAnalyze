





以下是最近约 20 轮排查/修复的“调试纪要”，方便集中回顾：

---

## 1. 首轮无输出 / 标签缺失
- **问题**：模型常以 emoji/Markdown 标题开头，后端无法识别 `<Analyze>`，导致首轮直接卡死。
- **修复**：新增标签归一化与标签闭合补救逻辑（@demo/backend.py#784-810、#812-829）。
- **效果**：任意 emoji/`## Analyze` 都会被转换为标准 `<Analyze>`，并自动补齐缺失的 `</Code>`。

## 2. 首轮 schema 不执行 / 路径找不到
- **问题**：工作区存在子目录（如 example/.../student_loan.sqlite），旧实现只在根目录找 `.sqlite`。
- **修复**：[iter_sqlite_files](cci:1://file:///d:/Python-Learning/deepanalyze/demo/backend.py:252:0-264:33) + 递归查找 + [run_schema_bootstrap](cci:1://file:///d:/Python-Learning/deepanalyze/demo/backend.py:897:0-909:44) 自动注入 `<Analyze>/<Code>/<Execute>`（@demo/backend.py#229-305、#842-909、#1090-1107）。
- **效果**：即使模型沉默，系统也会自动执行 `sqlite_master` 并把真实表结构注入上下文。

## 3. 反复查询 `sqlite_master` / 无法推进 EDA
- **问题**：模型倾向在多轮输出相同 schema 代码。
- **修复**：检测重复 `<Analyze>`/`<Code>`/`<Execute>`，连续两次 schema 重复即强制终止（@demo/backend.py#1120-1143、#1255-1283、#1519-1535）。
- **效果**：模型被提醒必须转向真实表，避免卡在 “列结构” 循环。

## 4. 代码完整性校验
- **问题**：模型输出的 `<Code>` 片段常缺 `import sqlite3`、`sqlite3.connect` 或只包含 SQL。
- **修复**：在执行前逐项检查 import/连接/plt.savefig 等约束，不满足则退票并提示（@demo/backend.py#1318-1360）。
- **效果**：保证传入 [execute_code_safe](cci:1://file:///d:/Python-Learning/deepanalyze/demo/backend.py:80:0-122:16) 的永远是完整脚本，减少子进程崩溃。

## 5. 真实表引用校验
- **问题**：模型凭空引用 `loan`/`borrower` 等不存在的表，Execution 报错后仍继续。
- **修复**：[extract_table_mentions_from_text](cci:1://file:///d:/Python-Learning/deepanalyze/demo/backend.py:301:0-313:25) + [extract_sql_table_names](cci:1://file:///d:/Python-Learning/deepanalyze/demo/backend.py:316:0-319:17) 判断 `<Analyze>` 是否引用已确认表，并在执行后追加提示（@demo/backend.py#302-333、#1127-1156、#1292-1308）。
- **效果**：模型一旦引用未知表，会在下一轮收到警告并被要求回到 `sqlite_master`。

## 6. 工作区文件追踪与重复文件命名
- **问题**：生成的图/CSV 会覆盖旧文件或无法被下载。
- **修复**：[snapshot_workspace_files](cci:1://file:///d:/Python-Learning/deepanalyze/demo/backend.py:322:0-327:20) + [uniquify_path](cci:1://file:///d:/Python-Learning/deepanalyze/demo/backend.py:364:0-387:14)；新增 [build_download_url](cci:1://file:///d:/Python-Learning/deepanalyze/demo/backend.py:154:0-159:42) 统一下载链接（@demo/backend.py#335-389、#105-160、#1418-1468）。
- **效果**：所有新文件会被复制到 `generated/` 并附上下载 URL。

## 7. 迭代控制 & 退票机制
- **问题**：纠错轮次也计入 `MAX_ITERATIONS`，导致任务尚未真正执行就结束。
- **修复**：[refund_iteration()](cci:1://file:///d:/Python-Learning/deepanalyze/demo/backend.py:850:4-852:41) 在每次提示后回退计数；同时记录 `raw_iterations` 防止死循环（@demo/backend.py#974-1004、#994-1019）。
- **效果**：模型犯错时不会“耗光票数”，但若反复犯同样错误仍会触发上限。

## 8. 循环停滞于 `<Answer>`
- **问题**：在无非 schema 执行结果时仍输出 `<Answer>`。
- **修复**：若 `non_schema_exec_rounds==0` 则忽略 `<Answer>` 并追加提醒（@demo/backend.py#1053-1068）。
- **效果**：只有完成至少两轮真实执行后才允许结论。

## 9. 文件声明与实际产物不一致
- **问题**：模型多次宣称生成 report/CSV，但 `generated/` 为空。
- **修复**：解析 `<File>` 中的文件名，执行后与真实新增文件比对，不匹配则提醒并退票（@demo/backend.py#1221-1497）。
- **效果**：杜绝“口头生成文件”，逼迫模型输出实际脚本。

## 10. 执行日志落地
- **问题**：子进程输出只在控制台可见，复盘困难。
- **修复**：每次执行后将 stdout/stderr 写入 `generated/execute_round_{n}.txt`（@demo/backend.py#1369-1390）。
- **效果**：即使前端无响应，仍可在工作区查到执行记录。

## 11. MAX_PROMPT_CHARS 配置化
- **问题**：环境变量未同步、部署差异导致报错。
- **修复**：`API/config.py` 中新增 `MAX_PROMPT_CHARS`，后端统一读取（@API/config.py、@demo/backend.py#134-143）。
- **效果**：不同环境可通过配置对齐上下文截断长度。

## 12. 迭代终止与答案补写
- **问题**：达到最大迭代或用户停机后没有 `<Answer>` 提示。
- **修复**：若任务被迫结束但仍无 `<Answer>`，自动补写告警（@demo/backend.py#1569-1575）。
- **效果**：前端能感知终止原因。

## 13. 结构化日志与 HTTP 端口
- **问题**：`workspace` 静态服务器与 backend 端口冲突。
- **修复**：将 HTTP server 绑定到 8100 并在后台启动（@demo/backend.py#137-187）。

## 14. 进程中止命令
- **问题**：用户无法中途停止长任务。
- **修复**：`/chat/stop` 设置 `SESSION_STOP_FLAGS`，[bot_stream](cci:1://file:///d:/Python-Learning/deepanalyze/demo/backend.py:809:0-1373:26) 循环中实时检查（@demo/backend.py#191-210、#1564-1568).

## 15. 代码执行 API session 隔离
- **问题**：执行其它会话代码时相互覆盖。
- **修复**：`/execute` API 根据 `session_id` 写入独立 workspace（@demo/backend.py#690-713）。

## 16. 递归 Workspace 文件信息
- **问题**：前端“上传文件信息”传递不完整。
- **修复**：[format_workspace_payload](cci:1://file:///d:/Python-Learning/deepanalyze/demo/backend.py:228:0-252:37) + [collect_file_info](cci:1://file:///d:/Python-Learning/deepanalyze/demo/backend.py:210:0-225:28)，并将其注入系统 prompt（@demo/backend.py#211-251、#943-969）。

## 17. Schema 摘要注入
- **问题**：即便成功运行 schema，模型仍忘记真实表。
- **修复**：首次确认 schema 后由系统追加 PRAGMA 摘要，指明下一步建议（@demo/backend.py#1396-1408）。

## 18. 未知表执行后错误提示
- **问题**：执行结果出现 `no such table` 时用户难以定位。
- **修复**：解析执行输出中的错误，并附带当前 schema 摘要，要求下一轮修复（@demo/backend.py#1534-1555）。

## 19. `<File>` 段统一生成
- **问题**：模型会在 `<Code>` 里写入 `with open()` 但不调用 `plt.savefig()`。
- **修复**：系统根据真实新增/修改文件生成 `<File>`，若没有文件则强制输出“暂无文件”（@demo/backend.py#1418-1468）。

## 20. 仍存在的核心风险
- **模型质量**：即便后端严控流程，只要模型持续拒绝生成合规脚本，仍会触发“提示—退票—再提示”的循环，看似“无输出”。  
- **缺乏运行日志**：若前端完全没有 `<Analyze>` 返回，需查看 `generated/execute_round_*.txt` 或后端控制台。没有这些信息无法定位停顿点。

## 21. 模型流式超时未捕获
- **问题**：vLLM 服务在流式阶段超时断开时，`bot_stream` 没有 try/except 包裹 `for chunk in response`，FastAPI 抛 `httpx.ReadTimeout` 后前端卡死且无提示.
- **修复**：为流式迭代增加异常捕获，输出 `<Answer>` 告警并终止当前任务，提示检查模型服务（@demo/backend.py#1038-1086）。
- **效果**：即使模型端超时，前端也能立即收到错误说明，避免长时间无响应并方便定位根因。

## 22. 模型伪造 `<File>` 链接导致空 CSV
- **问题**：模型在 `<File>` 中直接声明 `person_info.csv`、`bool_info.csv` 等文件，但脚本并未写入，后端的真实产物校验又只针对执行后的新增文件，导致前端展示了指向 `generated/` 下空文件的下载链接.
- **修复**：`bot_stream` 在每轮解析后先用 `extract_file_claims` 记录声明，再通过 `strip_model_file_blocks` 移除模型自带 `<File>` 段，仅保留系统根据真实文件生成的 `<File>`（@demo/backend.py#796-836、#1056-1105）。
- **效果**：页面上只会出现验证过的文件链接，若模型继续“嘴上生成”文件，会收到退票提醒，不会再看到空 CSV。

## 23. 未知表执行/绘图未保存导致结论失真
- **问题**：模型在 `<Analyze>/<Code>` 中引用 `student`/`students` 等并不存在的表，还在绘图时只 `plt.show()` 不保存文件，最终前端展示了没有依据的 `<Answer>` 和空 `<File>`.
- **修复**：当 schema 已确认但脚本引用未知表时立即提示并退票，禁止继续执行；同时若检测到 `plt.`/`sns.` 的绘图逻辑却缺少 `plt.savefig` 或 `plt.close`，直接提示补齐后再执行（@demo/backend.py#1316-1406）。
- **效果**：模型只能基于真实表推进分析，并且所有可视化都必须生成实际文件后才允许进入下一轮，避免“凭空 EDA”。

## 24. 模型伪造 `<Files>` 块导致用户误判产物
- **问题**：本轮模型使用自定义 `<Files> ... </Files>` 标记列出 `loans_distribution.png` 等文件，虽然实际并未生成，仍被前端当成 `<File>` 展示，造成 “generated 目录为空却显示有下载链接” 的假象.
- **修复**：统一将大小写不同的 `<Files>`/`</Files>` 正则替换成标准 `<File>` 标签，并在记录消息前调用 `strip_model_file_blocks`，彻底移除模型自带的 `<File>/<Files>` 段，仅保留系统根据真实产物生成的部分（@demo/backend.py#787-851、#1265-1273）。
- **效果**：页面上不会再出现模型伪造的文件列表，只有真正落盘的 PNG/CSV 才会在 `<File>` 中展示。

## 25. `<Analyze>` 无引用导致无穷循环
- **问题**：模型多轮 `<Analyze>` 仍不引用 `bool/person/enlist...` 等真实表，导致系统一再提醒“必须引用 sqlite_master 返回的表名”，看似卡死.
- **修复**：放宽 `require_known_reference` 条件——只有在已执行至少一轮真实 SQL (`non_schema_exec_rounds>0`) 后才强制 `<Analyze>` 提及真实表；并确保 `extract_table_mentions_from_text` 用大小写无关的比对避免误判（@demo/backend.py#314-327、#1152-1189）。
- **效果**：首轮提示更友好，待模型真正完成一次 EDA 后才启用强校验，避免“卡住”错判，同时仍可在后续阶段阻止凭空造表。

## 26. 生成文件链接 404
- **问题**：`person_name_distribution.png` 已写入 session 子目录，但下载链接仍指向 `generated/xxx`，未带 `session_id` 前缀，静态服务器找不到文件导致 404.
- **修复**：统一用 `WORKSPACE_ROOT` 计算文件相对路径（`workspace_relative_path`），并在 `bot_stream` 的 `<File>` 段中使用该路径构建 URL（@demo/backend.py#140-168、#1506-1517）。
- **效果**：所有下载链接都指向实际路径 `workspace/<session>/generated/...`，前端点击即可打开。

## 27. MAX_PROMPT_CHARS NameError
- **问题**：最新重启后日志显示 `NameError: MAX_PROMPT_CHARS is not defined`，因为重构时只在 config 中保留该配置，backend.py 丢失了默认值，导致接口直接 500.
- **修复**：在 backend 顶部补回 `MAX_PROMPT_CHARS = getattr(api_config, "MAX_PROMPT_CHARS", 16000)` 声明，与之前版本保持一致（@demo/backend.py#126-133）。
- **效果**：trim_messages 再次获得有效阈值，流式响应不会因缺失配置崩溃。

## 28. 模型误建临时表导致 schema 泛滥
- **问题**：在 schema 已确认后，模型输出 `CREATE TABLE student_id_x` 等 DDL，`list_sqlite_tables` 会把这些临时表纳入 `known_tables`，系统随后强制模型围绕这些假表分析，形成“表结构无穷无尽”的错觉.
- **修复**：1）初始化时记住 `baseline_tables` 并锁定 `known_tables`，只有首次 schema 扫描才能更新；2）执行前检测 `CREATE/DROP/ALTER TABLE`，一旦出现立即退票提示禁止修改结构（@demo/backend.py#314-336、#1014-1050、#1297-1324、#1466-1484）。
- **效果**：模型只能引用真实初始表，任何试图新增/删除表的脚本都会被拦截，避免再次出现几百个 `student_id_*` 表的伪造场景。

## 29. assistant 重复刷屏
- **问题**：模型流式输出会在标签前反复打印“assistant assistant…”，原逻辑未清理这些噪音，导致前端页面被成百上千个“assistant”填满，看似卡死.
- **修复**：在 `normalize_model_tags` 中新增 `ASSISTANT_ECHO_PATTERN`，对连续多次出现的“assistant”立即剔除，同时保持原有 emoji/标题/`<Files>` 归一化逻辑（@demo/backend.py#806-871）。
- **效果**：每轮消息写入前都会去除自重复 token，界面只保留真正的 `<Analyze>/<Code>…` 内容，不再被“assistant”刷屏。

## 30. 提前输出 `<Answer>` 导致无穷循环
- **问题**：在尚未执行任何真实 SQL/EDA (`non_schema_exec_rounds==0`) 时，模型多次直接给出 `<Answer>`，原逻辑只替换成 `<Answer (ignored)>` 并追加提示，却仍继续执行后续流程，导致前端不断收到同一段 `<Answer>`，会话无法前进.
- **修复**：1）新增 `premature_answer_rounds` 计数与 `premature_answer_detected` 标记，检测到非法 `<Answer>` 后立即 `refund_iteration()` 并跳过后续校验/执行；2）若连续 3 次违规则强制终止会话并提示原因，防止死循环（@demo/backend.py#1016-1141）。
- **效果**：模型在未完成 EDA 前若贸然输出 `<Answer>`，系统会即时退票并提醒，最多三次后自动中止，避免界面被重复 `<Answer>` 淹没，同时保障后续迭代可以继续进行.

## 31. `<File>` 虚报导致前端显示但 generated 目录无文件
- **问题**：模型在 `<File>` 中声明 `person_fields_info.csv`、`person_missing_values.png` 等文件，实际代码尚未执行（或执行被校验退票），后台仍把这段 `<File>` 原文透传给前端，用户点击后跳到模型伪造的 URL（甚至是 `https://example.com/...`），`workspace/<session>/generated/` 中也没有任何真实文件.
- **修复**：1）先对原始输出做标签归一，再用 `extract_file_claims` 捕捉模型声称的文件名，随后立即 `strip_model_file_blocks`，避免伪造链接进入消息流；2）新增 `emit_file_claim_warning`，若本轮被退票或执行后仍未发现对应文件，就在流式内容中追加 `<Analyze>` 告警并写回下一轮提示；3）真实执行成功时对 `workspace_path` 下新增/修改的所有文件重新复制到 `generated/`，确保 `<File>` 仅列出可下载的本地路径（@demo/backend.py#1133-1173、#1500-1609）。
- **效果**：前端只会展示经系统校验的真实下载链接，任何“嘴上生成”的文件都会立即被提示并阻断，点击后也不会再跳转到无关网站.

## 32. `sqlite3.connect` 指向不存在的数据库导致执行前即失败
- **问题**：模型在代码里频繁写 `sqlite3.connect('student_loan.db')`、`'enrolled_loan_amounts'` 等本地并不存在的路径，虽然系统要求“只用真实表名”，但旧逻辑只检测“是否出现 connect”，未核实路径是否有效，导致脚本在执行前就抛错、没有任何文件生成，却仍可能声称已输出图表.
- **修复**：新增 `SQLITE_CONNECT_PATTERN` 捕捉代码中的 `sqlite3.connect(...)` 参数，并与 `iter_sqlite_files(workspace_path)` 实际发现的 `.sqlite/.db` 列表比对；一旦路径不存在，立刻退票并提示真实数据库文件的相对路径示例，要求模型改用真实位置（@demo/backend.py#819-1501）。
- **效果**：只要模型尝试连接虚构数据库，系统会在执行前给出明确错误并退款轮次，保证后续 `<Execute>/<File>` 只能依赖真实文件，杜绝因为连错库导致的“有链接无产物”。

## 33. 流式输出提前露出 `<File>` 标签造成“有链接无文件”的错觉
- **问题**：虽然我们在每轮结束后会 `strip_model_file_blocks`，但流式调用 `client.chat.completions.create(..., stream=True)` 时仍把模型原始 `<File>` 片段直接推给前端；只要模型在流中杜撰 `person_age_distribution.png` 等文件，页面立刻出现下载按钮，而此时脚本尚未执行、`workspace/<session>/generated/` 也为空.
- **修复**：在流式循环中实时累积 `raw_res`，每次增量都先 `normalize_model_tags` 再调用 `strip_model_file_blocks`，确保任何未校验的 `<File>` 内容都不会出现在界面；迭代结束后再统一进入后处理（@demo/backend.py#1096-1116、#1151-1153）。
- **效果**：前端只会看到系统生成的 `<File>` 块。若模型声称产物却未真正写入，`generated/` 依旧为空，但也不会再露出假链接，问题定位清晰.

## 34. 流式阶段引用 `cur_res` 变量导致崩溃
- **问题**：为了在流式过程中阻止提前输出 `<Answer>`，旧代码直接在 `for chunk in response` 循环里访问 `cur_res`。然而 `cur_res` 只在流结束后才由 `normalize_model_tags(raw_res)` 赋值，因此在流中检测到 `<Answer>` 时会抛出 “cannot access local variable 'cur_res'” 的异常，误判为模型断流.
- **修复**：把流式循环中的引用切换为实时维护的 `sanitized_stream`，即 `current_stream = sanitized_stream`；只要模型在流中输出 `<Answer>`，就基于当前流内容进行替换和提示，完全避免访问未初始化的 `cur_res`（@demo/backend.py#1120-1138）。
- **效果**：后台不再出现 `cur_res` 未定义的报错，真正的流式中断仍会被捕获为 `stream_error`，错误提示与实际情况保持一致.

## 35. 模型任意声称文件但代码未执行导致“有名无实”
- **问题**：若 `<Code>` 在校验阶段被退票（如缺导入、引用不存在的库文件等），`claimed_files_in_round` 仍残留，下一轮提示时却没有明确指出“脚本未执行”。模型于是继续嘴上列出 `xxx.png`，用户看不到真实文件也不知道上一轮为何无产物.
- **修复**：在流式循环的 `finally` 中检测“宣称文件但本轮未执行任何代码”，立即输出系统提示，明确告知这些 `<File>` 只是声明，不会出现在下载列表（@demo/backend.py#1099-1747）。
- **效果**：一旦脚本被退票或压根没有执行，系统马上提示“本轮代码未执行或已被退票”，提醒模型/用户重新提交真实脚本，避免误以为文件失踪.

## 36. 误用 `information_schema` 等外部系统表导致脚本失败、文件缺失
- **问题**：有些轮次在 `<Code>` 中查询 `information_schema.columns`、`pg_catalog` 等非 SQLite 的元数据表，脚本执行立刻抛错，既没有 `<Execute>` 成功输出，也不会生成任何图表/CSV，最终 `<File>` 仍为“暂无文件”。
- **修复**：在解析 SQL 表名后新增 `PROHIBITED_TABLES` 白名单，对 `information_schema` / `pg_catalog` / `mysql` / `sys` 等关键字直接拦截，立即退票并提示“请改用 sqlite_master 中真实存在的表”。这一逻辑位于 @demo/backend.py#848-1493。
- **效果**：模型若再次引用这些外部系统表，会被即时提醒并回到真实的 SQLite 表做分析，避免执行阶段报错也避免再次出现“口头宣称文件但 generated 为空”的现象.

---
### 后续建议
1. **复测时收集日志**：每次卡住请提供 [demo/backend.py](cci:7://file:///d:/Python-Learning/deepanalyze/demo/backend.py:0:0-0:0) 控制台日志 + 当前 session 的 `generated/execute_round_*.txt`。没有日志就难以判断是模型没响应还是执行抛错。  
2. **观察 `<File>` 提示**：若看到“系统未检测到你在 `<File>` 中声明的文件…”，说明模型在虚报文件，这是预期的防护逻辑；此时需要重新运行等待模型给出真实脚本。  
3. **必要时重置会话**：若模型多轮仍不遵守结构，可清空 `workspace/<session>` 并重新发起，以避免上下文污染。  
4. **继续完善日志与告警**：若未来仍频繁出现“首轮即卡死”，建议在 [bot_stream](cci:1://file:///d:/Python-Learning/deepanalyze/demo/backend.py:809:0-1373:26) 里增加更多 `print` 或写入 `generated/` 的状态文件，以便快速定位.
4. **继续完善日志与告警**：若未来仍频繁出现“首轮即卡死”，建议在 [bot_stream](cci:1://file:///d:/Python-Learning/deepanalyze/demo/backend.py:809:0-1373:26) 里增加更多 `print` 或写入 `generated/` 的状态文件，以便快速定位。

目前我能提供的就是这一份汇总文档。若你能补充“本次重新执行后后端/`generated` 目录里出现了哪些内容”，我可以继续定位为何页面还停在初始提示.